function ComParametro({titulo, aluno, nota}){
    return (
        <div>
            <h2>{ titulo }</h2>
            <h3>{ aluno } tem nota { nota }</h3>
        </div>
    )
};

export default ComParametro;